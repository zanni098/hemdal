{
  "name": "hemdal",
  "private": true,
  "version": "0.1.0",
  "description": "Hemdal - Secure cross-platform password, secret, and variable manager with P2P sync",
  "scripts": {
    "dev:desktop": "pnpm --filter @hemdal/desktop dev",
    "build:desktop": "pnpm --filter @hemdal/desktop build",
    "dev:extension": "pnpm --filter @hemdal/extension dev",
    "build:extension": "pnpm --filter @hemdal/extension build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0"
}
